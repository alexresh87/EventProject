<div class="form-floating mb-3">
    <input type="text" name="{{ $name }}" class="form-control" id="{{$id}}" placeholder="{{ $title }}" value="{{ $value }}">
    <label for="{{$id}}">{{ $title }}</label>
</div>